package JavaProjects.ParkingProject;

//If these enumerations need to be extended, you can add and alter them, but doing so will require changing some code in other classes and logic.

public enum ParkingSlotSize{
	NORMAL,  BIG;
}
